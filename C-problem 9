#include <stdio.h>
int main()
{
    int i =5,even,f, s, sum;
    while(i <50)
    {
        even=2*i;
        i++;
        f=i%10;
        s=i/10;
        sum=f+s;
        if(sum==6)
        {
        printf("%d\n",i);
        }
    }
}
