#include <stdio.h>

int main()
{
    int a, b, c, lcm;
  printf ("enter the number;");
    scanf("%d%d%d", &a, &b, &c);

    lcm = a;

    while(1)
    {
        if(lcm % a == 0 && lcm % b == 0 && lcm % c == 0)
        {
            printf("%d", lcm);
            break;
        }

        lcm++;
    }
}
